<<<<<<< HEAD
name: Backend CI/CD

on:
  push:
    branches: [ main, develop ]
=======
name: Backend CI/CD Pipeline

on:
  push:
    branches: [ main, dev, dev-backend ]
>>>>>>> dev-backend
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'
  pull_request:
<<<<<<< HEAD
    branches: [ main, develop ]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'

env:
  PYTHON_VERSION: '3.13'
  POETRY_VERSION: '1.8.3'

jobs:
  lint:
    name: Code Quality & Linting
=======
    branches: [ main, dev, dev-backend ]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'
  workflow_dispatch:
    inputs:
      debug_enabled:
        description: 'Enable debug mode'
        required: false
        default: 'false'
        type: boolean

env:
  PYTHON_VERSION: '3.11'
  POETRY_VERSION: '1.6.1'

jobs:
  code-quality:
    name: Code Quality & Security
>>>>>>> dev-backend
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
<<<<<<< HEAD

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Cache Poetry installation
      uses: actions/cache@v3
      with:
        path: ~/.local
        key: poetry-${{ env.POETRY_VERSION }}-${{ runner.os }}

=======
      with:
        fetch-depth: 0
    
    - name: Set up Python
      id: setup-python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
    
    - name: Cache Poetry installer
      id: poetry-cache
      uses: actions/cache@v3
      with:
        path: ~/.local/share/pypoetry
        key: poetry-${{ runner.os }}-${{ env.POETRY_VERSION }}
    
    - name: Install Poetry
      if: steps.poetry-cache.outputs.cache-hit != 'true'
      uses: snok/install-poetry@v1
      with:
        version: ${{ env.POETRY_VERSION }}
        virtualenvs-create: true
        virtualenvs-in-project: true
    
    - name: Get pip cache dir
      id: pip-cache
      shell: bash
      run: |
        echo "dir=$(pip cache dir)" >> $GITHUB_OUTPUT
    
    - name: Cache pip dependencies
      uses: actions/cache@v4
      with:
        path: ${{ steps.pip-cache.outputs.dir }}
        key: ${{ runner.os }}-pip-${{ hashFiles('backend/pyproject.toml', 'backend/poetry.lock') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Cache Poetry virtual environment
      uses: actions/cache@v4
      with:
        path: backend/.venv
        key: ${{ runner.os }}-venv-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('backend/pyproject.toml', 'backend/poetry.lock') }}
        restore-keys: |
          ${{ runner.os }}-venv-${{ steps.setup-python.outputs.python-version }}-
    
    - name: Install dependencies
      working-directory: backend
      run: |
        poetry install --with dev,test
    
    - name: Code formatting check (Black)
      working-directory: backend
      run: |
        poetry run black --check --diff .
    
    - name: Import sorting check (isort)
      working-directory: backend
      run: |
        poetry run isort --check-only --diff .
    
    - name: Linting (Flake8)
      working-directory: backend
      run: |
        poetry run flake8 .
    
    - name: Type checking (MyPy)
      working-directory: backend
      run: |
        poetry run mypy .
    
    - name: Security scan (Bandit)
      working-directory: backend
      run: |
        poetry run bandit -r app/ -f json -o bandit-report.json || true
        poetry run bandit -r app/ -f txt
    
    - name: Dependency vulnerability scan (Safety)
      working-directory: backend
      run: |
        poetry run safety check --json --output safety-report.json || true
        poetry run safety check
    
    - name: Upload security reports
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: security-reports
        path: |
          backend/bandit-report.json
          backend/safety-report.json

  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: code-quality
    
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      id: setup-python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
>>>>>>> dev-backend
    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: ${{ env.POETRY_VERSION }}
        virtualenvs-create: true
        virtualenvs-in-project: true
<<<<<<< HEAD
        installer-parallel: true

    - name: Cache dependencies
      id: cache-deps
      uses: actions/cache@v3
      with:
        path: backend/.venv
        key: pydeps-${{ hashFiles('backend/pyproject.toml') }}-${{ env.PYTHON_VERSION }}

    - name: Install dependencies
      if: steps.cache-deps.outputs.cache-hit != 'true'
      working-directory: ./backend
      run: poetry install --no-interaction --no-ansi

    - name: Run Black (Code Formatting Check)
      working-directory: ./backend
      run: |
        poetry run black --check --diff .
      continue-on-error: false

    - name: Run isort (Import Sorting Check)
      working-directory: ./backend
      run: |
        poetry run isort --check-only --diff .
      continue-on-error: false

    - name: Run Flake8 (Style Guide Enforcement)
      working-directory: ./backend
      run: |
        poetry run flake8 --config=setup.cfg .
      continue-on-error: false

    - name: Run Pylint (Static Code Analysis)
      working-directory: ./backend
      run: |
        poetry run pylint app/ --rcfile=.pylintrc --output-format=text --reports=no --score=no
      continue-on-error: true  # Pylint warnings shouldn't fail CI

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: lint  # Tests only run if linting passes
=======
    
    - name: Cache Poetry virtual environment
      uses: actions/cache@v4
      with:
        path: backend/.venv
        key: ${{ runner.os }}-venv-${{ matrix.python-version }}-${{ hashFiles('backend/pyproject.toml', 'backend/poetry.lock') }}
        restore-keys: |
          ${{ runner.os }}-venv-${{ matrix.python-version }}-
    
    - name: Install dependencies
      working-directory: backend
      run: |
        poetry install --with dev,test
    
    - name: Run unit tests
      working-directory: backend
      run: |
        poetry run pytest tests/ -v \
          --cov=app \
          --cov-report=xml \
          --cov-report=html \
          --cov-report=term-missing \
          --junit-xml=test-results.xml \
          -m "unit or not integration and not performance and not security"
    
    - name: Upload test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: test-results-${{ matrix.python-version }}
        path: |
          backend/test-results.xml
          backend/htmlcov/
          backend/coverage.xml

  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: unit-tests
>>>>>>> dev-backend
    
    services:
      postgres:
        image: postgres:15
        env:
<<<<<<< HEAD
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: test_requify
=======
          POSTGRES_PASSWORD: testpass
          POSTGRES_USER: testuser
          POSTGRES_DB: testdb
>>>>>>> dev-backend
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
<<<<<<< HEAD

      redis:
        image: redis:7-alpine
=======
      
      redis:
        image: redis:7
>>>>>>> dev-backend
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
<<<<<<< HEAD

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Cache Poetry installation
      uses: actions/cache@v3
      with:
        path: ~/.local
        key: poetry-${{ env.POETRY_VERSION }}-${{ runner.os }}

=======
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      id: setup-python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
    
>>>>>>> dev-backend
    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: ${{ env.POETRY_VERSION }}
        virtualenvs-create: true
        virtualenvs-in-project: true
<<<<<<< HEAD
        installer-parallel: true

    - name: Cache dependencies
      id: cache-deps
      uses: actions/cache@v3
      with:
        path: backend/.venv
        key: pydeps-${{ hashFiles('backend/pyproject.toml') }}-${{ env.PYTHON_VERSION }}

    - name: Install dependencies
      if: steps.cache-deps.outputs.cache-hit != 'true'
      working-directory: ./backend
      run: poetry install --no-interaction --no-ansi

    - name: Set up test environment
      working-directory: ./backend
      env:
        DATABASE_URL: postgresql+asyncpg://test_user:test_password@localhost:5432/test_requify
        TEST_DATABASE_URL: postgresql+asyncpg://test_user:test_password@localhost:5432/test_requify
        REDIS_URL: redis://localhost:6379/0
        SECRET_KEY: test-secret-key-for-ci
        ALGORITHM: HS256
        ACCESS_TOKEN_EXPIRE_MINUTES: 30
        TESTING: true
      run: |
        # Create test environment file
        cat > .env.test << EOF
        DATABASE_URL=postgresql+asyncpg://test_user:test_password@localhost:5432/test_requify
        TEST_DATABASE_URL=postgresql+asyncpg://test_user:test_password@localhost:5432/test_requify
        REDIS_URL=redis://localhost:6379/0
        SECRET_KEY=test-secret-key-for-ci
        ALGORITHM=HS256
        ACCESS_TOKEN_EXPIRE_MINUTES=30
        TESTING=true
        EOF

    - name: Wait for services
      run: |
        sleep 10

    - name: Run database migrations
      working-directory: ./backend
      env:
        DATABASE_URL: postgresql+asyncpg://test_user:test_password@localhost:5432/test_requify
      run: |
        poetry run alembic upgrade head

    - name: Run unit tests
      working-directory: ./backend
      env:
        DATABASE_URL: postgresql+asyncpg://test_user:test_password@localhost:5432/test_requify
        TEST_DATABASE_URL: postgresql+asyncpg://test_user:test_password@localhost:5432/test_requify
        REDIS_URL: redis://localhost:6379/0
        SECRET_KEY: test-secret-key-for-ci
        TESTING: true
      run: |
        poetry run pytest tests/ -v \
          --tb=short \
          --cov=app \
          --cov-report=xml \
          --cov-report=html \
          --cov-report=term-missing \
          --cov-fail-under=80 \
          -m "unit" \
          --maxfail=5

    - name: Run integration tests
      working-directory: ./backend
      env:
        DATABASE_URL: postgresql+asyncpg://test_user:test_password@localhost:5432/test_requify
        TEST_DATABASE_URL: postgresql+asyncpg://test_user:test_password@localhost:5432/test_requify
        REDIS_URL: redis://localhost:6379/0
        SECRET_KEY: test-secret-key-for-ci
        TESTING: true
      run: |
        poetry run pytest tests/ -v \
          --tb=short \
          --cov=app \
          --cov-append \
          --cov-report=xml \
          --cov-report=html \
          --cov-report=term-missing \
          -m "integration" \
          --maxfail=3
      continue-on-error: true  # Integration tests can be flaky

    - name: Upload coverage reports
      uses: codecov/codecov-action@v3
      if: always()
      with:
        file: ./backend/coverage.xml
        directory: ./backend/htmlcov
        flags: backend
        name: backend-coverage
        fail_ci_if_error: false

    - name: Upload test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: test-results
        path: |
          backend/coverage.xml
          backend/htmlcov/
          backend/pytest.xml
        retention-days: 7

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: lint
=======
    
    - name: Install dependencies
      working-directory: backend
      run: |
        poetry install --with dev,test
    
    - name: Wait for services
      run: |
        sleep 10
        docker ps
    
    - name: Run integration tests
      working-directory: backend
      env:
        DATABASE_URL: postgresql://testuser:testpass@localhost:5432/testdb
        REDIS_URL: redis://localhost:6379/0
        TESTING: true
      run: |
        poetry run pytest tests/ -v \
          --cov=app \
          --cov-report=xml \
          --junit-xml=integration-test-results.xml \
          -m "integration"
    
    - name: Upload integration test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: integration-test-results
        path: |
          backend/integration-test-results.xml
          backend/coverage.xml

  api-tests:
    name: API Endpoint Tests
    runs-on: ubuntu-latest
    needs: unit-tests
>>>>>>> dev-backend
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
<<<<<<< HEAD

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

=======
    
    - name: Set up Python
      id: setup-python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
    
>>>>>>> dev-backend
    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: ${{ env.POETRY_VERSION }}
<<<<<<< HEAD

    - name: Install dependencies
      working-directory: ./backend
      run: poetry install --no-interaction --no-ansi

    - name: Run Safety (Security vulnerabilities check)
      working-directory: ./backend
      run: |
        poetry run pip install safety
        poetry run safety check --json --output safety-report.json || true
      continue-on-error: true

    - name: Upload security report
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: security-report
        path: backend/safety-report.json
        retention-days: 7

  build-check:
    name: Build Check
    runs-on: ubuntu-latest
    needs: [lint, test]
    if: always() && (needs.lint.result == 'success')
=======
        virtualenvs-create: true
        virtualenvs-in-project: true
    
    - name: Install dependencies
      working-directory: backend
      run: |
        poetry install --with dev,test
    
    - name: Run API endpoint tests
      working-directory: backend
      run: |
        poetry run pytest tests/ -v \
          --junit-xml=api-test-results.xml \
          -m "api or auth"
    
    - name: Upload API test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: api-test-results
        path: backend/api-test-results.xml

  performance-tests:
    name: Performance Tests
    runs-on: ubuntu-latest
    needs: integration-tests
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev'
>>>>>>> dev-backend
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
<<<<<<< HEAD

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

=======
    
    - name: Set up Python
      id: setup-python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
    
>>>>>>> dev-backend
    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: ${{ env.POETRY_VERSION }}
<<<<<<< HEAD

    - name: Build package
      working-directory: ./backend
      run: |
        poetry build

    - name: Upload build artifacts
      uses: actions/upload-artifact@v3
      with:
        name: build-artifacts
        path: backend/dist/
        retention-days: 7

  notify:
    name: Notify Results
    runs-on: ubuntu-latest
    needs: [lint, test, security-scan, build-check]
    if: always()
    
    steps:
    - name: Check workflow status
      run: |
        echo "Lint result: ${{ needs.lint.result }}"
        echo "Test result: ${{ needs.test.result }}"
        echo "Security scan result: ${{ needs.security-scan.result }}"
        echo "Build check result: ${{ needs.build-check.result }}"
        
        if [ "${{ needs.lint.result }}" != "success" ]; then
          echo "❌ Linting failed - check code formatting and style"
          exit 1
        fi
        
        if [ "${{ needs.test.result }}" != "success" ]; then
          echo "⚠️ Tests failed - check test results"
        fi
        
        echo "✅ Backend CI/CD pipeline completed"
=======
        virtualenvs-create: true
        virtualenvs-in-project: true
    
    - name: Install dependencies
      working-directory: backend
      run: |
        poetry install --with dev,test
    
    - name: Run performance tests
      working-directory: backend
      run: |
        poetry run pytest tests/ -v \
          --junit-xml=performance-test-results.xml \
          -m "performance"
    
    - name: Upload performance test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: performance-test-results
        path: backend/performance-test-results.xml

  build-and-deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest
    needs: [code-quality, unit-tests, api-tests]
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Build Docker image
      working-directory: deploy/docker
      run: |
        docker build -t requify-backend:latest -f backend/Dockerfile.prod ../../backend
    
    - name: Run container health check
      run: |
        docker run -d --name test-container -p 8000:8000 requify-backend:latest
        sleep 30
        curl -f http://localhost:8000/health || exit 1
        docker stop test-container
        docker rm test-container
    
    - name: Generate deployment summary
      run: |
        echo "## Deployment Summary" >> $GITHUB_STEP_SUMMARY
        echo "- **Branch**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Image**: requify-backend:latest" >> $GITHUB_STEP_SUMMARY
        echo "- **Status**: ✅ Successfully built and tested" >> $GITHUB_STEP_SUMMARY

  notification:
    name: Notify Results
    runs-on: ubuntu-latest
    needs: [code-quality, unit-tests, integration-tests, api-tests]
    if: always()
    
    steps:
    - name: Generate status report
      run: |
        echo "## Backend CI/CD Pipeline Results" >> $GITHUB_STEP_SUMMARY
        echo "- **Code Quality**: ${{ needs.code-quality.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Unit Tests**: ${{ needs.unit-tests.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Integration Tests**: ${{ needs.integration-tests.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- **API Tests**: ${{ needs.api-tests.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Branch**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
>>>>>>> dev-backend
